import { Card, Price, StatCard } from '@closed-commerce/ui';
import { loadAdminAnalytics } from '@/lib/admin-data';

export default async function AnalyticsPage() {
  const result = await loadAdminAnalytics();
  return <><div className="admin-heading"><div><p className="eyebrow">ATTRIBUTION / FUNNEL</p><h1>판매·추천인 통계</h1><p className="muted">Referral Code와 UTM 채널을 매출·가입·주문으로 연결해 봅니다.</p></div><span className={`badge ${result.source === 'supabase' ? 'badge-success' : 'badge-warning'}`}>{result.source}</span></div>{result.source === 'unavailable' ? <div className="admin-note">Supabase service role 환경변수를 설정하면 실제 Analytics가 표시됩니다.</div> : null}<div className="admin-grid-4"><StatCard label="Landing" value={`${result.funnel.landings}회`} detail="attribution event" /><StatCard label="Signup" value={`${result.funnel.signups}명`} detail="Referral 귀속 가입" tone="accent" /><StatCard label="Order" value={`${result.funnel.orders}건`} detail="취소·환불 제외" tone="success" /><StatCard label="전환율" value={result.funnel.landings ? `${(result.funnel.orders / result.funnel.landings * 100).toFixed(1)}%` : '0%'} detail="Landing → Order" /></div><div className="admin-grid-2"><Card><h2>Referral별 성과</h2><div className="table-wrap"><table className="data-table"><thead><tr><th>Code</th><th>Landing</th><th>가입</th><th>주문</th><th>매출</th></tr></thead><tbody>{result.referrals.map((row) => <tr key={row.code}><td><strong>{row.code}</strong></td><td>{row.landings}</td><td>{row.signups}</td><td>{row.orders}</td><td><Price amount={row.sales} /></td></tr>)}</tbody></table></div></Card><Card><h2>UTM Source</h2><div className="table-wrap"><table className="data-table"><thead><tr><th>Source</th><th>이벤트</th></tr></thead><tbody>{result.channels.map((row) => <tr key={row.source}><td>{row.source}</td><td>{row.events}</td></tr>)}</tbody></table></div></Card></div><div className="admin-section admin-note">추천관계 자체가 아니라 실제 Landing·가입·주문·매출을 기준으로 집계하며, Commission은 정산 화면에서 주문 snapshot으로 확인합니다.</div></>;
}
